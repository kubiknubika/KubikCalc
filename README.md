# KubikCalc 🦀 (Engineering Edition)

> **Project 1/100** in my "Zero to Hero" portfolio challenge.

Профессиональный инженерный калькулятор с **Обратной Польской Записью (RPN)**, написанный на **Rust**.
В отличие от обычных калькуляторов, использует стековую архитектуру, что позволяет вычислять сложные выражения без использования скобок.

![Rust](https://img.shields.io/badge/Made_with-Rust-orange?style=for-the-badge&logo=rust)
![License](https://img.shields.io/badge/License-MIT-blue?style=for-the-badge)

## ✨ Возможности (Features)

Этот проект — не просто `a + b`. Это полноценный математический движок:

*   **🔢 Стековая машина:** Неограниченная глубина вычислений.
*   **📐 Тригонометрия:** `sin`, `cos`, `tan` + обратные `asin`, `acos` + гиперболические.
*   **🎓 Алгебра:** Факториалы (`!`), Логарифмы (`ln`, `log`), Степени (`^`, `root`).
*   **🧠 Умные конвертации:** Перевод градусов в радианы (`deg`, `rad`).
*   **🛡 Безопасность:**
    *   Защита от деления на ноль.
    *   Безопасная обработка переполнения стека.
    *   Никаких "падений" (Graceful Error Handling).

## 🏗 Архитектура
Проект демонстрирует принципы **Clean Architecture** и модульности:

```text
src/
├── main.rs    # UI & Input Loop (Controller)
├── engine.rs  # Stack Logic & Math (Model)
└── errors.rs  # Custom Error Types (Error Handling)